[project]
name = "grounded-incident-analysis"
version = "0.2.4"
description = "Grounded incident analysis: detectors first, optional Groq rewrite, closed-book Q&A."
readme = "README.md"
requires-python = ">=3.12,<4.0"
authors = [{ name = "Amal V R" }]
keywords = [
    "incident-response",
    "root-cause-analysis",
    "observability",
    "logs",
    "metrics",
    "anomaly-detection",
    "ai-engineering",
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Environment :: Console",
    "Framework :: FastAPI",
    "Intended Audience :: Developers",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.12",
    "Topic :: Software Development :: Libraries :: Application Frameworks",
    "Topic :: System :: Logging",
]
dependencies = [
    "fastapi>=0.135.1,<0.142.0",
    "uvicorn[standard]>=0.52.4,<0.53",
    "pydantic>=2.13.5,<3.0.0",
    "pydantic-settings>=2.15.0,<3.0.0",
    "typer>=0.27.2,<0.28.0",
    "rich>=14.1,<16.0",
    "pyyaml>=6.0.2,<7.0.0",
    "httpx>=0.28.1,<0.29.0",
    "starlette>=1.6.0",
]

[project.urls]
Homepage = "https://github.com/Instincts11/grounded-incident-analysis"
Repository = "https://github.com/Instincts11/grounded-incident-analysis"
Documentation = "https://github.com/Instincts11/grounded-incident-analysis#readme"
Issues = "https://github.com/Instincts11/grounded-incident-analysis/issues"

[project.scripts]
incident-agent = "incident_agent.cli:app"

[tool.poetry]
packages = [{ include = "incident_agent", from = "src" }]

[tool.poetry.group.dev.dependencies]
pytest = ">=8.4.1,<10.0.0"
pytest-cov = ">=6.2.1,<8.0.0"
ruff = ">=0.16.6,<0.17.0"
mypy = ">=2.3.1,<3.0.0"
pre-commit = "^4.6.2"
types-pyyaml = "^6.0.12.20260815"

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP", "N", "ANN", "RUF"]
ignore = []

[tool.mypy]
python_version = "3.12"
strict = true
warn_unused_configs = true
mypy_path = "src"

[[tool.mypy.overrides]]
module = ["yaml"]
ignore_missing_imports = true

[tool.pytest.ini_options]
pythonpath = ["src"]
addopts = "--cov=src/incident_agent --cov-report=term-missing --cov-report=xml --cov-fail-under=85 -q"
testpaths = ["tests"]

[tool.coverage.run]
branch = true
source = ["src/incident_agent"]

[tool.coverage.report]
skip_empty = true
show_missing = true
fail_under = 85
omit = ["src/incident_agent/__init__.py"]

[build-system]
requires = ["poetry-core>=1.9.0"]
build-backend = "poetry.core.masonry.api"
